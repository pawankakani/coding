# test_application.py
import pytest

from core.utils import some_reusable_function

def test_reusable_function_returns_correct_output():
    """Test that the reusable function behaves as expected."""
    # Assuming the function has some return value or side effect to test
    assert some_reusable_function() is None # A placeholder assertion
    
    # You would replace this with a real test for your function's behavior